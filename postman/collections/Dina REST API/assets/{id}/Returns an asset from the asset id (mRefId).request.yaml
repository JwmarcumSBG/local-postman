$kind: http-request
url: "{{baseUrl}}/assets/:id"
method: GET
pathVariables:
  - key: id
    value: <uuid>
    description: (Required) Asset id (mRefId)
examples: ./.resources/Returns an asset from the asset id (mRefId).resources/examples
order: 3000
