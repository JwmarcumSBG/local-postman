$kind: http-request
url: "{{baseUrl}}/assets/:id/sync"
method: GET
pathVariables:
  - key: id
    value: <uuid>
    description: (Required) Asset id (mRefId)
examples: ./.resources/Returns the synchronize properties for an asset.resources/examples
order: 1000
