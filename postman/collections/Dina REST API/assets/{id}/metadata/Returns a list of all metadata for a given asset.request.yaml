$kind: http-request
url: "{{baseUrl}}/assets/:id/metadata"
method: GET
pathVariables:
  - key: id
    value: <uuid>
    description: (Required) Asset id (mRefId)
examples: ./.resources/Returns a list of all metadata for a given
  asset.resources/examples
order: 1000
