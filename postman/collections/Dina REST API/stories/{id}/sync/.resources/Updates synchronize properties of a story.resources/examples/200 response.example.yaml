$kind: http-example
request:
  url: "{{baseUrl}}/stories/:id/sync?action=activate"
  method: PUT
  headers:
    Content-Type: application/json
  queryParams:
    - key: action
      value: activate
      description: Trigger story synchronization
  pathVariables:
    id: ""
  body:
    type: json
    content: "{}"
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
