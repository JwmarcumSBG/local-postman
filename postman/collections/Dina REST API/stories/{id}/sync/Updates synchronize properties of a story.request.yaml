$kind: http-request
url: "{{baseUrl}}/stories/:id/sync?action=activate&action=activate"
method: PUT
headers:
  Content-Type: application/json
queryParams:
  - key: action
    value: activate
    description: Trigger story synchronization
  - key: action
    value: activate
    description: Trigger story synchronization
pathVariables:
  - key: id
    value: <uuid>
    description: (Required) Story id
body:
  type: json
  content: "{}"
examples: ./.resources/Updates synchronize properties of a story.resources/examples
order: 2000
