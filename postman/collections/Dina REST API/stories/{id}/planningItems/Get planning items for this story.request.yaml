$kind: http-request
description: Returns a list of planning items for this story
url: "{{baseUrl}}/stories/:id/planningItems"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: id
    value: <uuid>
    description: (Required) Story id
examples: ./.resources/Get planning items for this story.resources/examples
order: 2000
