$kind: http-request
description: Get a single schema by identifier
url: "{{baseUrl}}/mdfs/:id"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: id
    value: <uuid>
    description: "(Required) "
examples: ./.resources/Retrieve a single schema by id.resources/examples
order: 1000
