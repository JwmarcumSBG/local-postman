$kind: http-request
url: "{{mimirBaseUrl}}/config/api/v1/config"
method: GET
headers:
  Accept: application/json
scripts:
  - type: afterResponse
    code: pm.test('Status 200', function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 7000
