$kind: http-request
url: "{{baseUrl}}/api/v1/mdfs/:mdfId"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: mdfId
    value: <string>
    description: (Required) The ID of the MDF model
examples: ./.resources/Retrieve an MDF entry.resources/examples
order: 1000
