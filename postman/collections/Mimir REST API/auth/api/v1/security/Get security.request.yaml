$kind: http-request
description: |
  Retrieve a JSON object describing how to verify external authentication tokens
  that a tenant can use to access their resources in the Mimir API.
url: "{{baseUrl}}/auth/api/v1/security"
method: GET
headers:
  Accept: application/json
examples: ./.resources/Get security.resources/examples
order: 1000
