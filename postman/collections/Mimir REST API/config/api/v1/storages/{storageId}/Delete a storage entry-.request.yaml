$kind: http-request
name: Delete a storage entry.
description: Delete a storage entry.
url: "{{baseUrl}}/config/api/v1/storages/:storageId"
method: DELETE
headers:
  Accept: application/json
pathVariables:
  - key: storageId
    value: <string>
    description: (Required) The identifier string for storage.
examples: ./.resources/Delete a storage entry-.resources/examples
order: 2000
