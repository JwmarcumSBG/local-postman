$kind: http-request
description: Update a storage under the tenant
url: "{{baseUrl}}/config/api/v1/storages/:storageId"
method: PUT
headers:
  Accept: application/json
pathVariables:
  - key: storageId
    value: <string>
    description: (Required) The identifier string for storage.
examples: ./.resources/Update a storage under the tenant.resources/examples
order: 1000
