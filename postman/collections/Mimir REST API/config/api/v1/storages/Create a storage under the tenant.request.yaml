$kind: http-request
description: Create a storage under the tenant
url: "{{baseUrl}}/config/api/v1/storages"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "id": "<string>",
      "name": "<string>",
      "description": "<string>"
    }
examples: ./.resources/Create a storage under the tenant.resources/examples
order: 2000
