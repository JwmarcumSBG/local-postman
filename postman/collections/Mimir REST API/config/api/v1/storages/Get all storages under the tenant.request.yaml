$kind: http-request
description: Get all storages under the tenant
url: "{{baseUrl}}/config/api/v1/storages"
method: GET
headers:
  Accept: application/json
examples: ./.resources/Get all storages under the tenant.resources/examples
order: 3000
