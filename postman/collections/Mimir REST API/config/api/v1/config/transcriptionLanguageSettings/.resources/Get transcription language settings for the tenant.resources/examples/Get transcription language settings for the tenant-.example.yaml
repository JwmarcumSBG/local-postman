$kind: http-example
name: Get transcription language settings for the tenant.
request:
  url: "{{baseUrl}}/config/api/v1/config/transcriptionLanguageSettings"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "total": "<number>",
        "_embedded": {
          "collection": [
            {
              "languageCode": "<string>",
              "transcriptionService": "<string>",
              "label": "<string>"
            }
          ]
        }
      }
order: 1000
