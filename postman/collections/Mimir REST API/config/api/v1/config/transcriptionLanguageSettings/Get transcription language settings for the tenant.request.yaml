$kind: http-request
description: |
  Get transcription language settings for the tenant
url: "{{baseUrl}}/config/api/v1/config/transcriptionLanguageSettings"
method: GET
headers:
  Accept: application/json
examples: ./.resources/Get transcription language settings for the tenant.resources/examples
order: 1000
