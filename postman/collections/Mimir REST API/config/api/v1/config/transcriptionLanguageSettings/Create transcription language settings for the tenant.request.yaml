$kind: http-request
description: |
  Create transcription language settings for the tenant
url: "{{baseUrl}}/config/api/v1/config/transcriptionLanguageSettings"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: "{}"
examples: ./.resources/Create transcription language settings for the tenant.resources/examples
order: 2000
