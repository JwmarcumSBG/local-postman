$kind: http-request
description: |
  Get all the tenant ids list
url: "{{baseUrl}}/config/api/v1/tenantIds"
method: GET
headers:
  Accept: application/json
examples: ./.resources/Get tenant ids list.resources/examples
order: 1000
